<section class="showcase-list">
  {%- for post in collections.post -%}
    <a
      href="{{ post.url }}"
      class="showcase-list--section"
      data-img-lightness={{ post.data.imgLightness or 'dark' }}
    >
      <h2>{{ post.data.title }}</h2>
      {% image post.data.imgSrc, post.data.imgAlt %}
    </a>
  {%- endfor -%}
</section>